[project]
name = "satusehat-mock"
version = "0.1.0"
description = "Local stand-in for the Indonesian SATUSEHAT FHIR R4 platform"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = []

[tool.uv]
package = false